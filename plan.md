# План реализации недельного планировщика

План составлен строго по [ТЗ](tz.md). Реализация будет идти четырьмя законченными этапами: после каждого этапа остаётся работоспособный и проверенный результат. Автоматические проверки выполняются разработчиком; ручная проверка оставлена только для оценки внешнего вида и удобства.

## 1. Основа приложения и локальные данные — выполнено

**Результат этапа:** созданы `index.html`, `style.css` и `script.js`; страница запускается локально. Реализован единый формат данных в `localStorage`, безопасное чтение/сохранение, модель задачи, обработка повреждённых данных и календарные функции для рабочих дней.

В этот этап входят:

- структура локального хранилища `financeTaskPlanner.v1`, настройки темы и сворачивания выполненных;
- создание идентификаторов и валидация полей задачи;
- расчёт локальных дат, начала рабочей недели, ближайшего рабочего дня и проверка запрета выходных;
- сортировка по времени, приоритету и времени создания;
- базовый каркас страницы с русскими подписями и безопасное отображение пользовательского текста.

**Автопроверка:**

- проверить создание пустого хранилища, сохранение данных и их восстановление после перезагрузки страницы;
- проверить валидацию обязательных полей, ссылок, длительности, дат в прошлом и выходных;
- проверить расчёты недель и ближайшего рабочего дня для всех дней недели;
- проверить сортировку на наборе задач с совпадающим временем и разными приоритетами;
- подставить повреждённый JSON в `localStorage` и убедиться, что страница не падает и данные не перезаписываются.

**Ручная проверка:** не требуется.

### Запись выполнения — 24.08.2026

**Готовый результат:**

- Созданы отдельные `index.html`, `style.css` и `script.js`; страница запускается локально без внешних зависимостей.
- В `script.js` реализовано хранилище `financeTaskPlanner.v1`, пустое начальное состояние, безопасное чтение/сохранение и отказ от перезаписи повреждённых данных.
- Добавлены функции для проверки задачи, ссылки, времени, длительности, рабочих дней, начала недели, ближайшего рабочего дня и сортировки по ТЗ.
- Добавлена базовая русскоязычная страница со статусом локального хранилища. Полный экран планирования будет реализован на этапе 2.
- Пользовательский текст подготовлен для безопасного отображения через экранирование HTML.

**Автопроверка выполнена:**

- Проверены синтаксис JavaScript и 32 автоматические проверки: пустое хранилище, сохранение и повторная загрузка данных, все ключевые правила валидации, рабочие дни, расчёт недель, сортировка, создание задачи, экранирование и обработка повреждённого/недоступного хранилища.
- Страница открыта через локальный веб-сервер в браузере: базовая разметка и статус хранилища отображаются, ошибок и предупреждений в консоли нет.
- После перезагрузки браузер подтвердил восстановление ранее созданного начального состояния из `localStorage`.

**Нюансы реализации:**

- В проекте был прежний однофайловый прототип с встроенными стилями и логикой, не соответствующий ТЗ. Он заменён обязательной трёхфайловой структурой, чтобы дальнейшие этапы строились на единой модели данных.
- В ходе проверки уточнена обработка ошибки: повреждённый JSON теперь отдельно распознаётся как некорректные данные, а недоступность `localStorage` — как ошибка доступа. В обоих случаях существующее значение не перезаписывается.
- Обычный `node` не доступен в PATH среды разработки; для автопроверок использован встроенный runtime. Это не добавило зависимости в проект.

**Добавления сверх плана:** нет.

## Возврат задач, перенесённых прежним правилом — 25.08.2026

- По подтверждению пользователя добавлена однократная миграция данных: невыполненные просроченные задачи, которые старая версия автоматически переместила из прошедшего дня текущей недели в текущий день, возвращаются в исходную колонку.
- Возвращённая задача сохраняет метку «Просрочена» и исходную дату просрочки. Миграция не затрагивает выполненные задачи и не повторяется после успешного применения.
- ТЗ дополнено правилом миграции; версия скрипта увеличена до `20260825-5`, чтобы опубликованная страница получила это исправление.

**Автопроверка выполнена:**

- Проверен синтаксис JavaScript и форматирование изменённых файлов.
- Проверен возврат просроченной задачи из вторника в понедельник по её сохранённой исходной дате.
- Проверено, что миграция выполняется только один раз и фиксирует свою версию в `localStorage`.
- Проверено, что выполненная задача не возвращается; правила текущей и следующей недели для новых просрочек продолжают работать.

**Добавления сверх плана:** нет.

## 2. Основной интерфейс недели и работа с обычными задачами — выполнено

**Результат этапа:** пользователь может полноценно вести неповторяющиеся задачи: создавать, редактировать, копировать, выполнять, удалять, переносить и менять блок. Данные видны в панели «Сегодня» и в нижней полосе выбранной недели и сохраняются в браузере.

В этот этап входят:

- верхняя навигация по неделям и отдельная крупная панель «Сегодня»;
- четыре остальные карточки текущей недели без дублирования сегодняшнего дня; пять карточек при просмотре прошлой или будущей недели;
- два блока в каждом дне: «Текущие задачи» и «Стратегические задачи»;
- форма добавления и редактирования, быстрые кнопки «+», меню действий и подтверждение удаления;
- отметка выполнения, сворачиваемый раздел выполненных и отображение длительности;
- ручной перенос с автоматическим переходом к неделе назначения;
- расчёт и вывод показателя «Занято»;
- защита несохранённой формы при закрытии модального окна, нажатии Escape, обновлении или закрытии вкладки.

**Автопроверка:**

- в браузере пройти сценарии создания задач обоих типов, редактирования, копирования, смены блока, выполнения, возврата в невыполненные, удаления с отменой и удаления с подтверждением;
- проверить предзаполненные дату и тип при открытии формы из каждого места;
- после каждой операции обновить страницу и подтвердить сохранность результата в `localStorage`;
- проверить навигацию по предыдущей, текущей и будущей неделям, включая состав нижней полосы из четырёх или пяти дней;
- проверить пересчёт «Занято» при добавлении, изменении и переносе задач;
- проверить, что несохранённая форма не закрывается без подтверждения и что при тестовой перезагрузке срабатывает браузерное предупреждение.

**Ручная проверка:** оценить на компьютере, удобно ли читаются крупная панель «Сегодня», нижние карточки и форма задачи.

### Запись выполнения — 24.08.2026

**Готовый результат:**

- Стартовый экран заменён полноценным интерфейсом недели: верхней навигацией, крупной панелью «Сегодня» и карточками остальных рабочих дней.
- При открытии текущей недели в нижней полосе показываются четыре дня без дублирования «Сегодня»; для прошлой и будущей недель — все пять рабочих дней.
- В каждом дне созданы независимые блоки «Текущие задачи» и «Стратегические задачи», быстрые кнопки добавления и показатель «Занято».
- Реализованы создание, редактирование, копирование, смена блока, выполнение, возврат из выполненных, сворачивание завершённых, ручной перенос и удаление с подтверждением.
- Реализована форма со всеми полями обычной задачи из ТЗ, русскими сообщениями валидации, безопасным выводом пользовательского текста и открытием ссылок в новой вкладке.
- Реализована защита от случайного закрытия заполненной формы: подтверждение по кнопке, Escape и клику за пределами окна; также добавлен обработчик предупреждения браузера при перезагрузке или закрытии вкладки.
- Добавлены спокойные визуальные акценты трёх приоритетов, адаптивная вёрстка и уведомления о завершённых действиях.

**Автопроверка выполнена:**

- Проверены создание текущей и стратегической задач, корректные предзаполнения даты и типа из кнопки конкретного блока, расчёт длительности и отсутствие ошибок в консоли.
- Проверены редактирование задачи, изменение длительности и пересчёт «Занято», отметка выполнения, скрытие завершённых и возврат задачи в работу.
- Проверены смена блока, копирование, перенос на будущую неделю с автоматическим переходом к ней, навигация по неделям и сохранение данных после перезагрузки страницы.
- Проверены валидация выходного дня и подтверждение закрытия несохранённой формы; при отмене подтверждения форма остаётся открытой, при подтверждении закрывается.
- В браузере проверены текущая неделя с четырьмя нижними днями и будущая неделя с пятью днями; на ширине 1366 px сетка будущей недели состоит из пяти колонок.
- JavaScript проходит синтаксическую проверку, а в ходе браузерных сценариев ошибок и предупреждений в консоли не обнаружено.

**Нюансы реализации:**

- Встроенный браузер разработки не предоставил системное окно `beforeunload` для автоматического чтения, хотя обработчик установлен. Это поведение нужно один раз подтвердить вручную в обычном браузере.
- Действие удаления проверено до безопасного подтверждающего диалога и отмены удаления. Финальное подтверждение удаления не выполнялось в браузерной проверке, чтобы не удалять тестовые локальные данные через интерфейс.

**Добавления сверх плана:** нет.

## 3. Планирование недели: просрочка, повторения, фильтры и настройки — выполнено

**Результат этапа:** планировщик умеет автоматически обрабатывать просроченные задачи, создавать повторяющиеся задачи до даты окончания, фильтровать список и запоминать визуальные настройки.

В этот этап входят:

- перенос старых невыполненных задач при запуске на сегодня или ближайший понедельник с меткой «Просрочена» и без копии в старой дате;
- создание независимых экземпляров повторяющейся задачи по выбранным дням до даты окончания;
- независимое редактирование экземпляра и удаление будущих невыполненных экземпляров серии;
- комбинируемые фильтры по типу, приоритету и состоянию, включая сброс;
- светлая и тёмная темы с сохранением выбора;
- горячие клавиши `N` и `Escape` в предусмотренных ТЗ условиях.

**Автопроверка:**

- подставить задачи из прошедших будних дней и проверить автоперенос при запуске в будний день и в выходной; убедиться, что выполненные задачи не переносятся;
- проверить, что перенесённая на сегодня задача видна именно в панели «Сегодня» и отмечена просроченной;
- создать серию с несколькими днями недели, сверить точное число и даты экземпляров, затем проверить независимое редактирование одного экземпляра и удаление будущей части серии;
- проверить все фильтры по отдельности и в сочетаниях, а также сброс и отсутствие изменений в данных;
- переключить тему, свернуть выполненные задачи и убедиться после перезагрузки, что настройки сохранены;
- проверить работу `N` вне полей ввода и отсутствие срабатывания клавиши в текстовых полях, селекторах даты и времени.

**Ручная проверка:** оценить, достаточно ли заметна просрочка и комфортно ли различаются терракотовый, янтарный и бирюзовый приоритеты в обеих темах.

### Запись выполнения — 24.08.2026

**Готовый результат:**

- При запуске страницы все старые невыполненные задачи автоматически переносятся на сегодняшний рабочий день, а при открытии в выходной — на ближайший понедельник. Исходная дата сохраняется, задача получает метку «Просрочена» и не дублируется в прошлом дне.
- Добавлено создание повторяющихся задач: пользователь выбирает дни рабочей недели и дату окончания, после чего формируются независимые экземпляры одной серии.
- Экземпляр повторяющейся задачи редактируется независимо. Удаление такой задачи открывает подтверждение удаления будущих невыполненных экземпляров серии; выполненная история остаётся.
- Добавлены комбинируемые фильтры по блоку, приоритету и состоянию, включая просроченные, а также кнопка сброса.
- Добавлены светлая и тёмная темы с сохранением выбора в `localStorage`.
- Добавлена горячая клавиша `N` для новой задачи вне интерактивных элементов. Escape надёжно закрывает открытые окна, в том числе когда фокус находится внутри поля формы.

**Автопроверка выполнена:**

- Выполнены 24 проверки чистых функций: валидация повторений, генерация шести экземпляров на выбранные дни, отсутствие повторяющихся идентификаторов, перенос просрочек в будний и выходной день, исключение выполненных задач из переноса и удаление будущей части серии.
- В браузере проверено создание серии с понедельником, средой и пятницей, отображение экземпляров в текущей и следующей неделях, а также независимое изменение одного экземпляра.
- Проверены автоматический перенос задачи из прошлого рабочего дня в панель «Сегодня», метка «Просрочена» и комбинированный фильтр по состоянию, блоку и приоритету.
- Проверены фильтры высокого и выполненного приоритета/состояния, сброс фильтров, тёмная тема и восстановление темы после перезагрузки.
- Проверены горячая клавиша `N`, отсутствие её срабатывания при вводе текста, Escape в поле формы и отсутствие ошибок или предупреждений в консоли.
- Синтаксис JavaScript и отсутствие проблем форматирования дополнительно проверены автоматически.

**Нюансы реализации:**

- При проверке выяснилось, что нативное поведение Escape внутри текстового поля отличается в используемой браузерной среде. Добавлен явный обработчик Escape на уровне страницы; он дополняет, а не заменяет обработчики модальных окон.
- Для упрощённой модели повторений `seriesId` используется только для удаления будущих экземпляров. Изменение расписания существующей серии намеренно не реализуется: для него создаётся новая серия, как зафиксировано в ТЗ.
- Финальное подтверждение удаления серии в браузере не выполнялось, чтобы не удалять тестовые локальные данные; чистая функция удаления будущих экземпляров проверена автоматически.

**Добавления сверх плана:** нет.

## 4. Резервные копии, адаптивность и итоговая приёмка — выполнено

**Результат этапа:** готова автономная версия продукта, соответствующая всем критериям приёмки ТЗ: с безопасным экспортом/импортом, адаптацией под телефон и финальной проверкой пользовательских сценариев.

В этот этап входят:

- экспорт полного состояния в JSON-файл с заданным именем;
- проверяемый импорт с подтверждением, автоматическим скачиванием резервной копии до замены и отказом при неверном файле;
- финальная настройка адаптивной вёрстки, доступности клавиатурой, фокусов и подписей элементов;
- проверка пустых списков, длинного текста, отсутствующих необязательных полей и задач с разной нагрузкой.

**Автопроверка:**

- экспортировать данные, очистить тестовое хранилище, импортировать файл и сравнить восстановленные задачи и настройки с исходными;
- проверить, что перед успешным импортом инициируется загрузка автобэкапа, а некорректный JSON и отмена подтверждения не изменяют текущие данные;
- запустить полный набор сценариев из раздела 15 ТЗ и сверить результат с критериями приёмки раздела 17;
- проверить страницу в ширинах для компьютера, планшета и телефона: отсутствие горизонтального обрезания, доступность всех кнопок и корректное расположение дней;
- пройти основные действия с клавиатуры и убедиться, что интерактивные элементы имеют видимый фокус и доступные подписи.

**Ручная проверка:** открыть страницу на компьютере и телефоне и оценить только то, что нельзя объективно проверить скриптами: спокойствие дизайна, читаемость, удобство касаний и естественность повседневного сценария планирования.

### Запись выполнения — 24.08.2026

**Готовый результат:**

- Добавлен экспорт всех задач и настроек в JSON-файл с именем `planner-backup-YYYY-MM-DD.json`.
- Добавлен импорт JSON-резервной копии: файл проверяется до изменения данных, затем показывается понятное подтверждение.
- Перед подтверждённой заменой импорт создаёт отдельную резервную копию текущего состояния с именем `planner-before-import-YYYY-MM-DD-HHmmss.json`. Если такую копию подготовить нельзя, импорт отменяется.
- После успешного импорта данные сохраняются в `localStorage`, применяется сохранённая тема, фильтры сбрасываются, открывается текущая неделя и обрабатываются просроченные задачи.
- Закончена адаптивная вёрстка: на широком экране дни располагаются в ряд, на планшете — в две колонки, на телефоне — в одну. Добавлена доступная подпись скрытого выбора файла импорта.

**Автопроверка выполнена:**

- Выполнены 14 проверок формата резервных копий: сериализация, восстановление без потери данных, обработка ошибочного JSON и схемы, недопустимой версии и автоматический перенос просроченных задач после импорта.
- Выполнены 7 дополнительных проверок границ полей: максимальная длина названия и описания, необязательная длительность, допустимая HTTPS-ссылка и запрет небезопасного адреса.
- В браузере проверены видимость экспортной и импортной кнопок, успешное формирование экспорта по пользовательскому уведомлению и отсутствие ошибок в консоли.
- Проверена адаптивность: на ширине 1366 px текущая неделя имеет 4 колонки, будущая — 5; на 900 px — 2 колонки, на 390 px — 1 колонку. Горизонтального переполнения нет.
- Итоговая перезагрузка страницы прошла без ошибок и предупреждений консоли.

**Нюансы реализации:**

- Встроенная среда браузерной проверки не сообщает о завершении локальной загрузки файла, хотя экспортное действие сформировало подтверждающее уведомление и логических ошибок не выявлено. Сам факт появления файла нужно один раз проверить в обычном браузере.
- Загрузку файла через системный выбор файла не выполняли автоматически, чтобы не передавать локальный файл в браузерную среду проверки. Формат и проверка импорта покрыты чистыми автоматическими тестами.
- Если в корректной резервной копии отсутствуют отдельные необязательные настройки внутри `settings`, они безопасно восстанавливаются значениями по умолчанию.

**Добавления сверх плана:** нет.

## Изменение ТЗ после завершения этапов — 24.08.2026

Пользователь запросил пересмотр только визуальной структуры по приложенному референсу. В `tz.md` зафиксирована новая композиция: единая недельная матрица «тип задач + понедельник–пятница», две цветные строки для стратегических и текущих задач, шапка с легендой и карточкой диапазона недели, а также нижняя информационная полоса.

- Отдельная крупная панель «Сегодня» и нижняя полоса карточек дней исключены; сегодняшний день выделяется в своей колонке общей сетки.
- Рабочая неделя понедельник–пятница, набор функций задач, `localStorage`, фильтры, резервные копии и правила автопереноса не изменены.
- Суббота, поле заметок и отдельная сущность «Приоритеты недели» из референса не добавлены: их не было в согласованном ТЗ.
- Код в рамках этого изменения не изменялся. Четыре завершённых этапа относятся к предыдущей структуре интерфейса; перед новой реализацией потребуется добавить в план отдельный этап переработки интерфейса и повторно проверить все визуально затронутые сценарии.

## 5. Переработка интерфейса по референсу — выполнено

**Готовый результат:** интерфейс перестроен в недельную матрицу, похожую на приложенный референс. Добавлена суббота, начальные примеры задач, встроенные SVG-иконки и три нижние карточки.

### Запись выполнения — 24.08.2026

**Что сделано:**

- Рабочая неделя расширена до понедельника–субботы: суббота доступна при создании, переносе и повторении задач; воскресенье остаётся выходным и ведёт к понедельнику при автопереносе.
- Экран заменён на таблицу «Тип задач + шесть дней»: тёмно-синяя строка стратегических задач и зелёная строка текущих задач, с выделением текущего дня.
- Добавлены встроенные SVG-иконки календаря, цели, планшета со списком, звезды и галочки; внешние изображения и зависимости не использованы.
- В нижней части добавлены карточки «Условные обозначения», «Приоритеты недели» и «Заметки». Приоритеты выводят до трёх высокоприоритетных невыполненных задач выбранной недели; заметки сохраняются отдельно для каждой недели в `localStorage`.
- При первом запуске без данных, а также для ранее созданного пустого списка без признака начального наполнения, создаются 37 редактируемых примеров задач из референса для понедельника–субботы. Существующие непустые данные пользователя не заменяются.
- ТЗ синхронизировано с новой моделью данных и визуальной структурой.

**Автопроверка выполнена:**

- Проверен синтаксис JavaScript и отсутствие ошибок форматирования в изменённых файлах.
- Проверены рабочие дни: суббота разрешена, воскресенье запрещено; повторение по субботам создаёт экземпляры, а просроченная задача в воскресенье переносится на понедельник.
- Проверены 37 начальных задач, наличие стратегической и текущей задач в субботней колонке и трёх высоких приоритетов.
- Проверено сохранение недельных заметок в структуре резервной копии и их корректное восстановление.

**Ручная проверка:** открыть [index.html](index.html) в обычном браузере и оценить сходство с референсом: плотность матрицы на широком экране, читаемость названий задач, заметность иконок, а также удобство горизонтальной прокрутки на телефоне.

**Нюансы реализации:**

- Автоматическая визуальная проверка в подключённой браузерной среде недоступна для локального файла по её политике доступа. Логика и структура проверены автоматически; визуальная оценка остаётся ручной.
- Начальные примеры появляются только при полностью пустом `localStorage`; для повторного просмотра примеров можно очистить данные сайта в браузере или открыть страницу в новом профиле.

**Добавления сверх плана:** нет; все изменения выполнены по новому запросу пользователя и обновлённому ТЗ.

## Корректировка интерфейса по обратной связи — 24.08.2026

- Убрано дублирующее обозначение года в карточке диапазона недели: остаётся «2026 г.», без второй буквы «г.».
- Даты в заголовках дней увеличены, а фон шапок дней сделан более насыщенным голубым, чтобы они визуально отличались от стратегических карточек.
- Список задач в каждой ячейке теперь объединён в отдельную скруглённую карточку: голубую для стратегического блока и светло-зелёную для текущего, по образцу референса.

**Автопроверка:** синтаксис JavaScript, наличие исправленного формата недели, новых размеров/цветов заголовков и групповых карточек проверены автоматически; `git diff --check` не выявил ошибок форматирования.

**Ручная проверка:** оценить в браузере читаемость увеличенных дат и достаточность контраста между фоном колонки и карточкой задач.

## Исправление автопереноса в открытой вкладке — 25.08.2026

- Добавлена проверка смены дня при возврате фокуса на вкладку, её повторном отображении и раз в минуту. Невыполненные задачи переносятся и помечаются просроченными без ручного обновления страницы.
- Для опубликованной версии добавлены параметры версий у CSS и JavaScript, чтобы после публикации браузер запрашивал актуальные файлы, а не использовал прежний кэш.
- ТЗ дополнено этим правилом обработки просроченных задач.

**Автопроверка:** проверены синтаксис JavaScript, перенос задачи 24.08 на 25.08 с меткой просрочки, наличие механизмов проверки даты и параметров обновления кэша; `git diff --check` не выявил ошибок форматирования.

## Исправление начального наполнения среди недели — 25.08.2026

- Устранена причина исчезновения понедельничных демонстрационных задач при первом открытии планировщика во вторник: проверка даты больше не отбрасывает их при создании, и автоперенос перемещает их на вторник с меткой просрочки.
- Добавлена однократная миграция `referenceSeedVersion: 2` для ранее созданных наборов, где все семь понедельничных примеров уже были потеряны. Она восстанавливает только полный отсутствующий набор и не создаёт дубликаты.
- ТЗ дополнено правилами создания и миграции начальных примеров.

**Автопроверка:** проверены создание всех 37 примеров при первом открытии во вторник, восстановление ровно семи отсутствующих понедельничных задач, их перенос на 25.08 с признаком просрочки и отсутствие повторного дублирования; синтаксис JavaScript и форматирование файлов корректны.

**Дополнение публикации:** увеличена версия адреса `script.js` до `20260825-3`, чтобы браузеры, ранее открывавшие сайт, принудительно получили исправленный скрипт после новой сборки Pages.

## Корректировка правил просрочки — 25.08.2026

- Изменено правило обработки просроченных задач: в пределах текущей недели задача не меняет назначенный день и получает метку «Просрочена».
- В начале следующей недели незавершённая задача переносится целиком на такой же день недели новой недели; копия в прежней неделе не остаётся. Например, задача прошлого вторника переносится на вторник новой недели.
- Метка просрочки и первая дата просрочки сохраняются после такого переноса. Уведомления теперь различают простую отметку просрочки и перенос на новую неделю.
- ТЗ приведено в соответствие с новой логикой, а версия подключаемого скрипта увеличена до `20260825-4` для обновления опубликованной страницы без устаревшего кэша.
- Эта запись заменяет прежние правила автопереноса, описанные в более ранних записях истории разработки выше.

**Автопроверка выполнена:**

- Проверен синтаксис JavaScript и форматирование изменённых файлов.
- Проверено, что задача понедельника во вторник остаётся в понедельнике и получает признак просрочки.
- Проверен перенос задач прошлого понедельника и прошлой пятницы соответственно на понедельник и пятницу новой недели.
- Проверено, что в воскресенье задача текущей недели остаётся на месте, а в следующий понедельник переносится на одноимённый день новой недели.
- Проверено, что выполненная задача не помечается и не переносится.

**Ручная проверка:** после публикации открыть страницу, оставить невыполненную задачу в прошлом дне текущей недели и убедиться, что в ней появилась метка «Просрочена», без смены колонки. Проверку переноса на следующую неделю можно выполнить, изменив системную дату либо дождаться начала новой недели.

**Добавления сверх плана:** нет.
